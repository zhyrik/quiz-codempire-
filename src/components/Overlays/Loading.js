import React from 'react'

import './Loading.css'

/**
 * functional react component for Loading component
 * @function
 * @returns {JSX.Element} - react component
 */
function Loading() {
  return (
    <div className="loading">Loading&#8230;</div>
  )
}

export default Loading